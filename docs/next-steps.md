# 다음에 할 것

2026-07-29 기준. 3단계(쓰기/PR)와 진행 표시까지 배포 완료된 시점에서 정리한 것.

이 문서는 날짜별 기록이 아니라 살아있는 목록이다. 하나 끝낼 때마다 지우고, 새로
생기면 여기에 붙인다. `docs/review-2026-07-29.md`가 "그때 무엇을 봤나"라면 이건
"이제 무엇을 하나"다.

## 지금 상태

원래 그리던 그림은 다 돈다. 초안을 던지면 봇이 위키에 이미 있는지 찾아보고,
카테고리를 정하고, 컨벤션대로 다듬어서 PR을 연다. 본문은 원문 그대로다 — 확인하고
어디에 어떻게 넣을지만 챙긴다. 기다리는 5~20초 동안 스레드엔 "답을 쓰는 중…"이
떠 있고, 머지는 사람이 한다.

던지는 방법은 둘이다. 옵시디언 `.md`를 슬랙에 **첨부**하거나, 글을 코드블록에
붙여넣거나. 첨부 쪽이 손해가 없다 — 슬랙 입력창이 마크다운을 먹지 않고, 그가 적어둔
프론트매터가 그대로 살아 온다. 대신 `files:read` 스코프와 토큰 재발급이 한 번 든다.

실측으로 확인된 것: 질문 하나에 `turns=2 in=18117 out=164`, 5.5초. Haiku 4.5
기준 약 2센트. `assistant.threads.setStatus`는 `chat:write`만으로 통과한다 —
2026-03-05 슬랙 변경이 실제로 적용돼 있다는 걸 curl로 직접 확인했다.

## 북극성 — 저장소가 곧 옵시디언 Vault

이 아래 모든 항목은 이 한 문장으로 판정한다. **"옵시디언에서 열었을 때 멀쩡한가?"**

새로 정한 게 아니라 이미 `CONVENTIONS.md` 첫 줄에 적혀 있는 것이다. "GitHub에서
읽히는 위키이자, 나중에 옵시디언 Vault로 그대로 쓰기 위한 규칙." `docs/obsidian.md`엔
설정까지 정리돼 있고 거기 "**Use [[Wikilinks]]: OFF** ← 제일 중요"라고 박혀 있다.
노트 47개가 이미 그 규칙대로 쌓여 있다.

그래서 봇의 일은 "볼트 호환으로 만들기"가 아니다. **이미 호환인 걸 깨뜨리지 않기**다.
훨씬 좁고, 그래서 검사할 수 있다. 봇이 지켜야 할 것들:

표준 마크다운 링크만 쓴다(`[제목](path.md)`). 파일명은 영문 kebab-case, 한글·공백
금지. 파일은 옮기지 않는다 — `archived`로 바뀌어도 자리는 그대로다. 프론트매터는
`title`·`aliases`·`created`·`updated`·`tags`·`status` 형식을 지킨다. 새 카테고리는
직접 만들지 않고 PR로 제안만 한다(이것도 `CONVENTIONS.md`에 이미 명시돼 있다).
하단 `## 관련 문서`가 백링크 대체다.

아래는 전부 "없어도 지금 쓰는 데 지장 없는" 것들이다. 순서는 값어치 대비 품 기준.

## 끝난 것 — 슬랙 `.md` 첨부 (2026-07-29)

옵시디언에서 쓰던 `.md`를 슬랙에 그대로 던지면 된다. 붙여넣기 경로도 그대로 산다.

**들어간 것:** `event.files[]` 파싱, `url_private`을 봇 토큰으로 GET, `.md`만 통과,
128KB 상한. 다운로드는 **워커**가 한다 — 게이트웨이는 3초 안에 응답해야 하고 워커는
300초를 쥐고 있다. 첨부가 여럿이면 첫 `.md` 하나만 쓰고 나머지는 "안 읽은 첨부"로
답에 적는다.

**본문은 파일 그대로다.** 어디까지가 본문인지 추측하던 `draftBody`가 이 경로엔 필요
없다 — 메시지는 지시, 파일은 내용으로 갈라졌으니까. 승인도 슬랙에 친 말에서만 읽는다.
파일 *안에* "올려줘"라고 적혀 있어도 그건 초안을 옮겨 적다 남은 줄일 뿐이다.

**프론트매터는 그의 것이 이긴다.** 모델이 낸 `title`·`status`·`tags`·`aliases`는 그가
*비워둔 칸*에만 들어가고, 채운 칸은 건드리지 않는다. `tags`는 합치지도 않는다 —
옵시디언에서 일부러 뗀 태그가 PR마다 되살아나는 것만큼 사람을 지치게 하는 게 없다.
채운 칸이 있으면 PR 본문에 적어 diff로 보이게 한다.

**옵시디언 전용 문법은 세기만 한다** (`internal/vault`). `[[위키링크]]`, 콜아웃,
`%% 주석 %%`, `![[임베드]]`를 찾아 개수를 슬랙 답과 PR 본문에 적는다. 고치지 않는다.
GitHub이 알아서 렌더하는 `[!NOTE]`·`[!TIP]`·`[!IMPORTANT]`·`[!WARNING]`·`[!CAUTION]`은
세지 않는다 — 안 깨지는 걸 깨진다고 하면 경고 자체를 안 믿게 된다.

**계획과 달라진 것 하나.** 위에선 "그대로 올릴까, 바꿔서 올릴까?"를 되묻기로 했는데,
봇은 메시지 사이에 기억이 없다. 되물으면 그가 답하는 순간 초안은 이미 사라진 뒤다.
그래서 되묻지 않고 **알리기만 한다** — 그 알림은 PR 본문에 실려 가고, PR이 이미 결정
지점이다. 바꿀 게 있으면 머지 전에 브랜치에서 고치면 된다. 판단은 사람이 한다는 건
그대로다.

**남은 손일 (우드로).** `files:read` 스코프 추가 → Reinstall → 새 `xoxb-`를 `.env`에
(에디터로, `echo` 말고) → `./deploy.sh`. 스코프가 없으면 슬랙이 이벤트에서 `files[]`를
통째로 빼고 보내서 봇은 파일이 온 줄도 모른다 — 에러조차 안 난다. 토큰 교체와 코드
배포를 한 배포에 묶지 말 것. 깨졌을 때 어느 쪽인지 안 갈린다.

## 1. 검색 품질 — 한국어 조사

제일 싼데 체감은 즉시 온다.

"동시성을 정리한 거 있어?"라고 물으면 "동시성"이 제목인 노트를 못 찾는다. 조사가
붙은 채로 매칭하기 때문이다. 을/를/이/가/은/는/에/의 정도만 떼어내도 대부분
해결된다. 형태소 분석기까지 갈 필요는 없다.

같이 볼 것: alias와 tag에 가중치를 주는 것. 지금은 본문 매칭과 제목 매칭이 같은
무게라, 본문에 한 번 스친 노트가 제목이 정확히 맞는 노트보다 위에 오는 일이 있다.

`internal/wiki` 안에서 끝나고, 새 스코프도 배포 위험도 없다. 테스트도 쓰기 쉽다.

## 2. 리뷰에서 남은 세 가지

`docs/review-2026-07-29.md`에서 나왔지만 아직 손대지 않은 것들이다. 셋 다 작다.

**(a)** `docs/note-style.md` §8에 한 줄 추가 — §5와 §6의 내용이 코드의
`systemPrompt()`에 복사돼 있다는 사실. 지금은 문서를 고쳐도 프롬프트는 안 바뀌는데,
그걸 알아챌 장치가 아무 데도 없다. 한 줄이면 다음 사람(=몇 달 뒤의 너)이 안다.

**(b)** 묵은 `seedling` 노트를 물어볼 수 있게 하기. "석 달째 seedling인 거 뭐 있어?"
같은 질문. 상태 사다리를 만들어놨는데 정작 안 자란 걸 찾는 길이 없다.

**(c)** 초안이 여러 주제에 걸칠 때 검색을 두 번 하게 하기. 지금은 한 번 찾아보고
없으면 새 노트로 간다. 초안이 두 주제를 섞고 있으면 한쪽만 보고 판단하는 셈이다.

## 3. 프롬프트 캐싱 — 숫자는 나왔다

"실측 나오면 판단하자"고 미뤄뒀던 게 이거고, 이제 숫자가 있다.

질문 하나에 입력 18,117토큰. Haiku 4.5 입력이 $1/MTok이니 질문당 약 1.8센트,
출력까지 합쳐 2센트가 안 된다. 이 중 상당 부분이 시스템 프롬프트와 툴 정의라
매 턴 똑같이 다시 올라간다. 캐싱이 깎는 게 정확히 그 부분이고, 캐시 읽기는
$0.10/MTok이니 열 배 차이다.

**그런데 절대액이 작다.** 하루 20질문이면 월 $11 남짓이고, 캐싱해도 월 몇 달러
아끼는 수준이다. 지금 순위가 낮은 이유가 이거다. 질문 수가 눈에 띄게 늘거나
초안 정리 모드를 자주 쓰게 되면 그때 다시 보면 된다.

## 4. MCP 분리 — 학습용

당장의 필요가 아니라 배우려고 하는 것이다.

읽기 절반(`internal/wiki`의 네 개 툴 — `search_wiki`, `list_notes`, `read_note`,
`wiki_status`)이 싸고 안전한 시작점이다. 슬랙에 안 묶여 있고, 실패해도 잃을 게 없다.

쓰기 절반은 그렇게 간단하지 않다. `goAhead`와 `draftBody`가 슬랙 대화 모양을
전제로 만들어져 있어서, MCP로 빼려면 승인 게이트를 다시 설계해야 한다. PR이
승인 게이트라는 원칙은 안 바뀌니까, "누가 무엇을 승인하는가"를 슬랙 밖에서
어떻게 표현할지가 실제 문제다.

## 5. PR을 폰에서 읽기 좋게

리액션 머지 대신 나온 항목이다. 위 "안 바꾸는 규칙"의 마지막 문단이 배경이다.

지금은 PR이 열리면 스레드에 링크와 짧은 한마디가 간다. 폰에서 그것만 보고는 열어볼
가치가 있는지 판단이 안 된다. 어디에 넣는지, 어떤 카테고리인지, 새 노트인지 기존
노트 수정인지, status와 태그가 뭔지 — 이 네댓 줄이 스레드에 같이 있으면 폰에서
바로 갈린다. 깃허브 앱의 머지 버튼은 어차피 한 번 탭이라, 남은 마찰은 누르는 게
아니라 파악하는 쪽이다.

이미 `propose_note`가 구조화된 값으로 받아 오는 것들이라 새로 알아낼 건 없다.
render 층에서 한 번 더 풀어 쓰면 된다. 새 스코프도, 새 권한도 없다.

## 정하고 갈 것

위키 저장소(`memories`)의 `projects/wiki-assistant.md`가 아직 untracked다.
커밋할지, 지울지, 그대로 둘지 한 번 정하면 끝난다.

## 안 바꾸는 규칙

새로 뭘 만들 때마다 다시 흔들리기 쉬운 것들이라 여기 적어둔다.

머지는 사람이 한다. 👍 리액션으로 머지하는 건 두 번 검토했고 두 번 다
**안 하기로 했다.** 세 번째로 고민하지 않도록 이유를 적어둔다.

가장 큰 건 이거다. **지금 봇은 머지를 못 한다** — 머지 API를 구현하지 않았다.
`main`을 건드리는 게 금지된 게 아니라 불가능하다. 리액션 머지를 붙이는 순간 그
능력이 생기고, 그때부터는 코드가 막던 자리를 규칙이 막게 된다. 능력이 없는 것과
능력은 있는데 안 쓰기로 한 것은 안전의 급이 다르다.

신호 자체도 나쁘다. 👍는 "좋다"일 수도 "봤다"일 수도 "고마워"일 수도 있다.
제스처는 싼데 결과는 위키에 영구히 남는 커밋이다. 채널에 있는 누구나 누를 수 있어서
권한을 코드가 아니라 슬랙 UI에 맡기게 되고, 모바일에선 리액션 피커가 작아 오탭도
난다. 무엇보다 diff를 안 보고 승인하게 된다 — PR이 존재하는 이유가 그건데.

편한 게 아쉬울 때 고칠 자리는 **머지를 쉽게** 만드는 쪽이 아니라 **PR을 읽기 쉽게**
만드는 쪽이다. 깃허브 모바일 앱엔 이미 머지 버튼이 한 번 탭이라, 실제 격차는
"누르기"가 아니라 "폰에서 뭘 바꾸는 건지 파악하기"다. 아래 6번이 그 항목이다.

머지 API는 만들지 않는다. 봇이 `main`에 직접 쓰는 코드 경로는 없고, 브랜치는
반드시 `bot/`으로 시작한다.

권한 확인은 툴 실행 직전 코드에서 한다. 프롬프트로 시키지 않는다. 읽기 툴과 쓰기
툴은 분리된 상태를 유지한다. 시크릿은 환경변수에만 둔다.

탐지는 넓게, 삭제는 좁게. 이 비대칭은 실수로 생긴 게 아니라 의도한 것이다.

## 다음 세션에 잃어버리면 아까운 것

세션이 끝나면 대화는 사라지니까, 다시 알아내려면 품이 드는 것만 적어둔다.

`assistant.threads.setStatus`는 `chat:write`로 호출된다. 슬랙 Agents 가이드는
Agents 기능 토글이 필요하다고 쓰여 있고 2026-03-05 변경 로그는 아니라고 읽히는데,
**실측 결과 토글 없이 `{"ok":true}`가 나왔다.** 문서가 엇갈릴 때는 curl 한 번이
빠르다.

이 저장소에 git 명령을 브리지 마운트로 돌리지 말 것. `git status`도 인덱스를
갱신하느라 `.git/index.lock`을 잡는데, 마운트가 파일 삭제를 막아놔서 락이 유령으로
남는다. 실제로 한 번 당했다.

`device_stage_files` 스냅샷은 동기화 확인에 쓰지 말 것. 오래된 내용을 돌려준 적이
있다. 마운트를 직접 읽거나 Filesystem MCP로 확인하는 게 맞다.

배포는 git과 무관하다. `deploy.sh`는 작업 트리에서 바로 빌드하니 커밋 안 해도
현재 코드가 올라간다. 반대로 `serverless deploy`가 "No changes to deploy"라고
하면 정말 안 올라간 거라, 로그의 침묵을 성공으로 읽으면 안 된다.

`files:read` 없이 파일을 첨부하면 슬랙은 이벤트에서 `files[]`를 **통째로 빼고**
보낸다. 401도, 빈 배열도 아니고 필드 자체가 없다. 봇 로그엔 아무 흔적이 안 남으니
"왜 무시하지?"로 한나절 헤맬 수 있다. 그리고 스코프가 있어도 토큰이 옛것이면
`url_private`이 **HTTP 200에 HTML 로그인 페이지**를 돌려준다 — 에러 코드가 아니다.
그래서 `download.go`가 Content-Type과 본문 앞머리를 같이 본다.

GitHub은 `[!NOTE]`·`[!TIP]`·`[!IMPORTANT]`·`[!WARNING]`·`[!CAUTION]` 다섯 개를
대소문자 무시하고 알림 상자로 렌더한다. 즉 옵시디언 콜아웃 전부가 깨지는 게 아니라
`[!info]`·`[!todo]`처럼 이 목록 밖의 것만 깨진다. `internal/vault`가 이 다섯을
빼고 세는 건 그래서다.
